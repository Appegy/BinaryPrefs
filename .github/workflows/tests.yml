name: Tests

on: [pull_request, workflow_dispatch]

jobs:
  test:
    name: EditMode tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Appegy.BinaryPrefs.Lab/Library
          key: Library-Tests-${{ hashFiles('Appegy.BinaryPrefs.Lab/Assets/**', 'Appegy.BinaryPrefs.Lab/Packages/**', 'Appegy.BinaryPrefs.Lab/ProjectSettings/**', 'src/**') }}
          restore-keys: |
            Library-Tests-
            Library-

      - name: Run EditMode tests
        uses: game-ci/unity-test-runner@v4
        id: tests
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          projectPath: Appegy.BinaryPrefs.Lab
          unityVersion: auto
          testMode: EditMode

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: ${{ steps.tests.outputs.artifactsPath }}
